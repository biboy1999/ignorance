import { Map as YMap } from "yjs";
import { YElement } from "../types/yjs";

export const initEvent = (
  _yelements: YMap<YElement>,
  _cy: cytoscape.Core
): void => {
  // for process each event
  // EventRegister.addEventListener("add", rawAddElementsEvent);
  // EventRegister.addEventListener("remove", rawRemoveElementsEvent);
  // EventRegister.addEventListener("raw data change", rawDataChangeEvent);
  // processed event
  // EventRegister.addEventListener("add", addElementsEvent);
  // EventRegister.addEventListener("remove", removeElementsEvent);
};
